$kind: http-request
url: "{{base_url}}/{{api_version}}/settlement/OFRY33CmgH"
method: GET
headers:
  - key: X-REST-KEY
    value: "123"
    disabled: true
auth:
  type: bearer
  credentials:
    token: "{{customer_access_token}}"
order: 1000
