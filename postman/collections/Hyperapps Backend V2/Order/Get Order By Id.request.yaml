$kind: http-request
url: "{{base_url}}/{{api_version}}/order/118751"
method: GET
headers:
  X-Restaurant-Id: "{{restaurant_id}}"
auth:
  type: bearer
  credentials:
    token: "{{customer_access_token}}"
order: 4000
