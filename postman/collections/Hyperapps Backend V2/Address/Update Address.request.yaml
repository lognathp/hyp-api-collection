$kind: http-request
url: "{{base_url}}/{{api_version}}/address/100009"
method: PATCH
body:
  type: json
  content: "{\r

    \      \"addressType\": \"OTHERS\"\r

    }"
auth:
  type: bearer
  credentials:
    token: "{{customer_access_token}}"
order: 2000
