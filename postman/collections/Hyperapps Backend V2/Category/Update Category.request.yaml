$kind: http-request
url: "{{base_url}}/{{api_version}}/category/lfHAEknmBb"
method: PATCH
headers:
  X-Restaurant-Id: "{{restaurant_id}}"
body:
  type: json
  content: |-
    {
        "active":"0"
    }
auth:
  type: bearer
  credentials:
    token: "{{restaurant_admin_access_token}}"
order: 2000
